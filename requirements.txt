fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pyserial>=3.5

# Real Carrera CU hardware backend (serial + BLE via AppConnect). Not
# needed to run the app in mock mode.
carreralib>=1.0.3

# Local physical gamepad support (Xbox controllers etc). Not needed for
# browser/keyboard/touch control.
pygame>=2.5

# Testing
pytest>=8.0
httpx>=0.27
websockets>=12.0
